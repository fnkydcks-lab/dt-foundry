# Backlog

Living list. Add to it, prune it, promote from it. See `/backlog`.

---

## Open

### Full end-to-end dogfood pass, /brief through /wild + /drift
- **Added:** 2026-08-13
- **Surfaced by:** conversation about the pack's own identity after merging /backlog, /wild, /drift
- **Why deferred:** the pack has never run start to finish on one real project — every prior pass has been partial. The upcoming new build is the first candidate, but it hasn't started yet, so this isn't actionable today.
- **What it resolves:** whether the engineering-lens cluster (`/qa`, `/trim`, `/zombie`, `/llmcodecheck`, `/dev`, `/sFlutterCheck`) is genuine role-filling for a solo founder (the working theory, backed by Chrrpt's real in-market gaps) or dilution of the pack's original human-first identity (`/brief`, `/taste`, `/persona`, `/room`). The honest answer only exists after a real run — this entry is the reminder to actually look, not to theorize about it again.

---

## Closed

*(nothing yet)*
